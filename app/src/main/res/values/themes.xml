<resources xmlns:tools="http://schemas.android.com/tools">

    <!-- Bas-tema för hela appen -->
    <style name="Base.Theme.WellnessQuest" parent="Theme.Material3.DayNight.NoActionBar">
        <!-- Tvinga NavigationView att inte tona ikoner -->
        <item name="navigationViewStyle">@style/CustomNavigationView</item>
    </style>

    <!-- Din egentliga apptema -->
    <style name="Theme.WellnessQuest" parent="Base.Theme.WellnessQuest" />

    <!-- Stil override för NavigationView -->
    <style name="CustomNavigationView" parent="Widget.Material3.NavigationView">
        <item name="itemIconTint">@null</item>
    </style>

</resources>
